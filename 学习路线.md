# 皇室战争旧版本私服 · 学习路线（仅限个人学习）

> 目标：理解并搭建一个 Clash Royale 老版本（推荐 **1.9.x，2017 年**）的私服，用来学习
> 游戏协议、服务端架构与逆向知识。**仅供学习，不要公开分发客户端/资源，不要搭建公开服务器。**

## 1. 总体思路

私服 = 老版本客户端 + 自建服务端。

- 客户端负责画面渲染与玩家操作；
- 服务端负责账号、数据存档、匹配与战斗逻辑（皇室战争的战斗是服务端计算的）；
- 两者通过 Supercell 自定义的二进制协议通信：
  - 主服务器：**TCP**（登录、主界面、部落、商店……）
  - 战斗服务器：**UDP**（对战过程中的实时同步）
  - 消息体采用 PiranhaMessage 帧格式，payload 用 **RC4 加密**，内部是类 protobuf 的二进制字段（VInt / 字符串等）。

## 2. 两条路线

| 路线 | 说明 | 难度 | 建议 |
|---|---|---|---|
| A. 跑通现成开源私服 | 下载 1.9.2 客户端 + RetroRoyale 系服务端，先玩起来，再边玩边读代码 | 低 | **推荐先做** |
| B. 从零实现 | 抓包、逆向协议，自己写消息层/登录/home/战斗 | 高 | 完成 A 之后选做 |

## 3. 环境与工具

必装：

- **.NET 8 SDK**（本机目前只有 .NET runtime，没有 SDK；HashRoyale 需要它）
- **MySQL 8**（本机未装；可用 Docker 或 XAMPP）
- **Android 模拟器**（MuMu / BlueStacks / LDPlayer）或一台安卓手机
- **Clash Royale 1.9.2 客户端**（原版 APK + 补丁版客户端，战斗功能需要补丁客户端）

逆向学习工具（可选，阶段 2 用）：

- `jadx` / `apktool`：反编译 APK，看客户端代码与资源
- `Wireshark`：抓包看 TCP/UDP 消息
- `Frida` / `mitmproxy`：Hook 客户端、篡改流量

你机器上已有 Java 24、Node 24、Python 3.13，可以用于读替代实现或写辅助脚本。

## 4. 参考项目

| 项目 | 语言/框架 | 目标版本 | 特点 | 用途 |
|---|---|---|---|---|
| [HashRoyale](https://github.com/Hashmane/HashRoyale) | C# / .NET 8 | 1.9.2 | ZrdRoyale 的改进版：聊天命令、多管理员、封禁、修正的 CSV | **推荐起点** |
| [RetroRoyale（原版）](https://github.com/retroroyale/ClashRoyale) | C# / .NET Core 3.1 | 1.9.0–1.9.3 | 生态最成熟，wiki 有战斗补丁教程 | 读文档、对比代码 |
| [1.9.x 服务器 fork（含客户端）](https://github.com/hostedbyjustus/Clash-Royale-1.9.x-Server-based-on-retroroyale) | C# / .NET Core | 1.9.x | 附带客户端与若干修复 | 找客户端/修复 |
| [AstralRoyaleLegacy](https://github.com/Greedycell/AstralRoyaleLegacy) | C# / .NET Core | 1.9.x | RetroRoyale 分支，已停更但可读 | 对比阅读 |
| [UCR（Ultrapowa Royale）](https://github.com/MrARM/UCR) | C# | 2016 早期版本 | 年代最早、功能不完整 | 了解早期协议 |
| [cr-private-server](https://github.com/tarik02/cr-private-server) | Java | 见其 README | Java 实现 | 语言对比 |
| [clashy](https://github.com/eaglesarecooler/clashy) | C# | 1.9 附近 | 轻量服务端 + 代理 + 客户端 | 学协议精简版 |
| [herol](https://github.com/topwo/herol) | Java | 仿 CR 的 Demo | 模拟 CR 玩法的简易服务端 | 学战斗玩法逻辑 |

## 5. 核心协议速览（结合 RetroRoyale 代码）

### 消息帧（PiranhaMessage）

```
ID（2 字节） | 长度（3 字节） | 版本（2 字节） | payload（RC4 加密）
```

关键类：`Protocol/PiranhaMessage.cs`（Decode/Encode/Process 生命周期）、
`Utilities/Netty/Reader.cs & Writer.cs`（VInt、ScString 等编码）。

### 登录流程

1. 客户端发 `ClientHelloMessage`；
2. 客户端发 `LoginMessage`（ID 10101，携带账号、token、客户端版本、fingerprint 哈希）；
3. 服务端校验 fingerprint → 查库登录 → 回 `LoginOkMessage`（ID 20104）；
4. 紧接着下发 `OwnHomeDataMessage`（玩家主城数据）、`AvatarStreamMessage`（消息流），有部落再下发 `AllianceStreamMessage`。

### 内容补丁（fingerprint）

`fingerprint.json` 记录资源版本哈希；客户端登录时若指纹不一致，服务端返回错误码并给
`PatchUrl`，客户端从该地址下载资源补丁。改游戏数据（CSV）后需要同步更新指纹。

### 命令系统

玩家的操作（部署卡牌、升级、开宝箱、改名……）封装成 LogicCommand，
打包进 `EndClientTurnMessage` 发送；服务端用 `LogicCommandManager` 按命令 ID 分发处理。

### 战斗

- 独立进程 `ClashRoyale.Battles`，走 UDP；
- 匹配成功后通过 `UdpConnectionInfoMessage` 告知客户端 UDP 地址；
- 战斗中交换 `BattleEventMessage`、`SectorStateMessage` 等；
- 客户端需要打补丁才能把战斗流量导向私服。

### 游戏数据

卡牌数值、角色、法术等都在 `csv_logic/` 与 `csv_client/` 的 CSV 文件中，
服务端启动时加载成对象（`Files/CsvLogic/`）。想改数值从这里入手。

## 6. 分阶段学习计划

### 阶段 1：跑起来（约 1–2 天）

1. 安装 .NET 8 SDK 和 MySQL（或 Docker 跑 MySQL）；
2. `git clone https://github.com/Hashmane/HashRoyale.git`；
3. `cd src/ClashRoyale && dotnet publish`；
4. 先运行一次生成 `config.json`，填好数据库连接、补丁地址、端口；
5. 启动主服；启动战斗服（`ClashRoyale.Battles`）；
6. 模拟器安装 1.9.2 客户端（补丁版），登录建号；
7. 验收：能登录、看到主界面、能进训练场/对战。

### 阶段 2：读懂架构（约 1–2 周）

- 顺着 `LoginMessage → LoginOkMessage → OwnHomeDataMessage` 读一条完整链路；
- 读 `PiranhaMessage.cs`、RC4 实现、Netty Reader/Writer，搞懂字段编码；
- 读 `EndClientTurnMessage` 与 `LogicCommandManager`，理解命令如何分发；
- 用 Wireshark 抓官方服与私服的包对比，理解加密与消息结构；
- 改一张卡的数据（CSV）并更新 fingerprint，让客户端生效，理解补丁机制。

### 阶段 3：改造与扩展（按兴趣）

- 数值/卡牌调整、送钻石、全卡解锁；
- 聊天命令、管理员、封禁（HashRoyale 已内置一部分）；
- 匹配逻辑、机器人对手；
- 部落功能、排行榜。

### 阶段 4：从零实现（选做）

- 用 Frida/jadx 逆向 1.9.2 客户端，整理消息 ID 与字段定义；
- 用自己熟悉的语言（Node/Java/Python）实现：消息帧 → RC4 → 登录 → home；
- 逐步补命令与战斗模拟器；
- 参考 `herol` 学习战斗玩法逻辑。

## 7. 注意事项

- **仅限学习**：只在自己的设备/局域网里跑；
- **不要公开分发**修改过的 APK、游戏资源、客户端；
- **不要搭建公开服务器或收费运营**；
- Supercell 对私服态度严格，公开传播有法律风险，请保持低调的个人学习用途。

## 8. 当前进度（2026-08-14 已搭建并启动）

已在本地搭建并运行 **HashRoyale（Clash Royale 1.9.2）** 私服：

- 代码：`HashRoyale/`（本仓库内克隆，含两处本地小补丁：无控制台环境下挂起等待改为永久运行）
- 编译产物：`HashRoyale/app/`（主服，net8.0）、`HashRoyale/app_battles/`（战斗服，netcoreapp3.1 + 运行时滚动到 8）
- 数据库：MySQL 8.4.9 便携版（`%USERPROFILE%\cr-tools\mysql`，数据目录 `cr-tools\mysql-data`），库 `rrdb2`，已导入 `database.sql`；root 空密码（仅本机）
- 端口：主服 TCP 9339（游戏客户端）、TCP 9876（集群）；战斗服 UDP 9449
- 启动/停止：运行 `.\start-server.ps1` / `.\stop-server.ps1`
- 日志：`HashRoyale/main-server.log`、`HashRoyale/battle-server.log`

### 下一步：接客户端

1. 下载 Clash Royale **1.9.2 补丁版客户端**（战斗功能需要补丁版，见 RetroRoyale wiki）；
2. 安装 Android 模拟器（MuMu / BlueStacks）；
3. 模拟器内客户端连服务器地址：模拟器中访问宿主机用 `10.0.2.2`，即 `10.0.2.2:9339`；
4. 登录建号，验证主界面与战斗流程。

## 9. 客户端与模拟器安装包（2026-08-14 已找到）

### 游戏客户端 APK

- 已下载到本机：`clients/ClashRoyale-1.9.2.apk`（94,972,386 字节，SHA256 `6A87C0F2FF47B886C099A195AEC242E88CDCA40BF6993095DB0826BCA320F0FA`）
- 官方来源（GitHub Release，原版未补丁）：
  <https://github.com/hostedbyjustus/Clash-Royale-1.9.x-Server-based-on-retroroyale/releases/tag/Clients>
  - 同 release 里还有 1.9.0 / 1.9.1 / 1.9.3 的 Android APK 与 1.9.0 的 iOS ipa
- 该 APK 同时包含 `armeabi-v7a`（真机）与 `x86`（模拟器）两个 ABI
- 客户端内服务器地址：`game.clashroyaleapp.com`（`lib/armeabi-v7a/libg.so` 与 `lib/x86/libg.so`）

### 连接私服需要做的两处客户端补丁

1. **服务器地址**：把 `libg.so` 里的 `game.clashroyaleapp.com` 替换成本机地址（等长或补 `\0`），如模拟器用 `10.0.2.2`，真机用局域网 IP `192.168.3.65`；
2. **战斗校验**（1.9.2）：按 RetroRoyale wiki 用十六进制编辑器改 `lib/armeabi-v7a/libg.so`：
   - 偏移 `0x001C27F0`：`D0` → `D1`
   - 偏移 `0x001C2B40`：`90` → `92`
   - （x86 版的对应偏移需另行分析）

改完后需要重新签名（apksigner / uber-apk-signer）才能安装。

### Android 模拟器

- MuMu 模拟器（网易）：<https://mumu.163.com/>
- BlueStacks 5：<https://www.bluestacks.com/download.html>
- LDPlayer 雷电模拟器：<https://www.ldplayer.net/>

## 10. 打通记录（2026-08-15：真机登录成功 ✅）

### 结论：客户端必须用 RetroRoyale 补丁版

- 官方 1.9.2 客户端（只改服务器地址）**无法登录**：它会发 `ClientHello(10100)` 后等服务端回
  `ServerHello(20100)`（会话密钥），而 HashRoyale/RetroRoyale 服务端不实现该握手；
- 正确的客户端是 **RetroRoyale 补丁版**（包名 `com.retrocell.clashroyale`）：
  - 跳过 `ClientHello`，直接发 `LoginMessage(10101)`（RC4 加密）；
  - RC4 密钥 = `fhsd6f86f67rt8fw78fw789we78r9789wer6re` + `nonce`（丢弃前 45 字节密钥流），
    与服务端 `HashRoyale/app/config.json` 的 `encryption_key` 一致；
  - 内置服务器地址为已关闭的 `cluster.retroroyale.xyz`，需改成 `192.168.3.65`。

### 已生成的文件

- `clients/retroroyale-1.9.2.apk`：从 <https://retroroyale.en.malavida.com/android/> 下载的原版补丁客户端；
- `clients/retroroyale-1.9.2-phone.apk`：已将 `libg.so` 中 `cluster.retroroyale.xyz` 替换为
  `192.168.3.65`（等长补 `\0`），并用 `zipalign + apksigner`（debug 密钥）重新签名。

### 实测结果（红米 K60，Wi-Fi 局域网）

1. 启动 `one-click-start.ps1` → MySQL(3306) + 主服(TCP 9339/9876) + 战斗服(UDP 9449)；
2. 手机安装 `retroroyale-1.9.2-phone.apk`（MIUI 会弹 USB 安装确认，需点“继续安装”）；
3. 启动游戏 → 直接 `LoginMessage` → 服务端建号（MySQL `rrdb2.player` 出现 Id=1）；
4. 接受服务条款 → 取名（`CodexPlayer`）→ 进入主界面：卡组、免费宝箱、皇冠宝箱、“对战”按钮；
5. 点“对战”进入“搜索对手”匹配流程（单机无第二个玩家，匹配不到对手属预期）。

### 后续可做的事

- 双开/第二个设备登录，验证 1v1 对战（战斗服务器 UDP 9449 已在跑）；
- 把客户端地址改成模拟器 `10.0.2.2` 可上模拟器；
- 战斗所需的“troop 校验补丁”（wiki：`0x001C27F0` D0→D1、`0x001C2B40` 90→92）本客户端若未内置，
  需按相同思路补上再重签。

## 11. 匹配 Bot（2026-08-15 已上线 ✅）

### 原理

- `tools/cr_bot.py` 是一个**协议级 bot**：以独立账号（MySQL `rrdb2.player` Id=2）登录主服，
  登录后立刻发 `EndClientTurn(14102)` + `StartMatchmakeCommand(525)` 进入 1v1 匹配队列，常驻等待；
- 玩家点“对战”时，服务端把玩家与队列里的 bot 配对成 `LogicBattle`，双方都会收到
  `UdpConnectionInfoMessage(24112)` 和 `SectorStateMessage(21903)`；
- bot 随即用 UDP 向战斗服（9449）发 1400 字节注册包，之后每 2 秒发一条
  `SectorCommandMessage(12904)`（0 条命令），让战斗服的 `BattleActive` 保持新鲜，战斗不会因空闲被提前结束；
- 90 秒后 bot 停止发命令 → 战斗服判 bot 空闲 → 主服给 bot 发 `BattleResultMessage(20225)` →
  bot 收到后立刻重新排队，实现“始终在搜索”。

### 配套服务端补丁

- 主服原本把战斗 UDP 地址填成节点 IP（本机为 `127.0.0.1`），手机连不上；
  新增 `udp_host` 配置（`HashRoyale/app/config.json` 设为 `192.168.3.65`），
  `LogicBattle.Start()` 优先用该地址下发 `UdpConnectionInfoMessage`。

### 使用

- 一键启动脚本会自动拉起 bot（`python tools\cr_bot.py`，日志在 `tools\cr_bot.log`）；
- 手机点“对战”即可秒配；打完（三冠/停手）后约 5~8 秒出结算，bot 同步重排；无人操作时 180 秒兜底结束；
- bot 账号保存在 `tools\bot_state.json`，删除该文件可让 bot 重新注册新账号。

## 12. 战斗协议修复（2026-08-15 三冠结算已正常 ✅）

### 踩过的三个坑（都在 `ClashRoyale.Battles`）

1. **UDP 命令块 RC4 失步 → 下兵无效/开局秒胜**
   - 根因：客户端会重传 UDP 包（同一 chunk seq 发两遍），服务端把重传当新块又解密了一次，
     RC4 流从此错位，后面所有命令全部乱码，`SectorCommandMessage` 解析抛
     `IndexOutOfRangeException`；
   - 修复：`SessionContext.Process` 记录 `LastIncomingSeq`，遇到重复 seq 的块跳过（不消耗 RC4 流）。
2. **保活包误刷“战斗活跃” → 战斗永不结束**
   - 客户端战斗时每 ~2 秒发一个 count=0 的命令块（这才是活跃信号），10 字节保活包不算；
   - 修复：只有真正处理命令块才刷新 `BattleActive`（`UdpMessageProcessor` 不再对保活包设置）。
3. **胜负判定**
   - 该 fork 战斗服不模拟战斗（塔血由客户端本地计算），三冠后客户端停止发命令块；
   - 修复：任一玩家超过 5 秒无命令块（且开战超过 10 秒）→ 给**双方**发 `BattleFinishedMessage`，
     双双结算并让 bot 立即重排；另加 180 秒整场兜底。

### 验证

- 下兵正常（DoSpellCommand type=1 被处理、重传被跳过、零报错）；
- 三冠后 5~8 秒正常弹出“获胜者！”结算页，bot 同时收到结果并重新排队；
- 战斗可跑满 3 分钟（不操作时按 180 秒兜底结算）。

### bot 自动出牌（2026-08-15）

- `tools/cr_bot.py` 现在会在战斗中**每 5 秒随机下一张牌**：随机卡牌（class 26、
  instance 0~64）、随机位置（自己半场）、随机等级（1~13），不看圣水、不看卡组；
- 报文结构对齐真实客户端：`[checksum][tick][count=1][type=1][clientTick][checksum][senderHi][senderLo][deck][class][instance][spellIdx][level][x][y]`，
  服务端按 `DoSpellCommand` 处理后转发给对手客户端。
